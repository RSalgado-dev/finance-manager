<% title = local_assigns[:title] %>

<section class="ui-card">
  <% if title.present? %>
    <h2 class="text-base font-semibold text-slate-950"><%= title %></h2>
  <% end %>
  <div class="<%= "mt-3" if title.present? %> text-sm leading-6 text-slate-700"><%= content %></div>
</section>
